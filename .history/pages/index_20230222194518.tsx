import { useLoadScript } from "@react-google-maps/api";
import Map from "../components/map";

export default function Home() {
  const { isLoaded } = useLoadScript();
  googleMapsApiKey: ProcessingInstruction.env,

  return <div>Map</div>;
}
